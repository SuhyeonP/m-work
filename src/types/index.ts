export interface IFilterContent {
  value: string | number;
  content: string | number;
}

export interface ISummaryContent {
  name: string;
  aliases: string;
  title: string;
  books: number;
  tvSeries: number;
  id: string | number;
}
